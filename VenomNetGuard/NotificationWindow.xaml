<Window x:Class="VenomNetGuard.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Venom Alert" Height="110" Width="380"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        WindowStartupLocation="Manual" FontFamily="Consolas">

    <Border x:Name="MainBorder" Background="#121216" BorderBrush="#FF003C" BorderThickness="1" CornerRadius="3" Padding="15">
        <StackPanel>
            <TextBlock x:Name="TxtTitle" Text="[ CRITICAL ALERT ]" Foreground="#FF003C" FontWeight="Black" FontSize="14" Margin="0,0,0,5"/>
            <TextBlock x:Name="TxtMessage" Text="Neznámá IP se pokouší o přihlášení..." Foreground="#C5C5D0" FontSize="12" TextWrapping="Wrap"/>

            <Grid Margin="0,10,0,0">
                <TextBlock x:Name="TxtTime" Text="LIVE INTERCEPT" Foreground="#6A6A7A" FontSize="10" HorizontalAlignment="Left"/>
                <TextBlock x:Name="TxtCounter" Text="ZACHYCENO: 1" Foreground="#00A2FF" FontWeight="Bold" FontSize="10" HorizontalAlignment="Right"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>